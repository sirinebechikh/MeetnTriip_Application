<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des réclamations</title>
    <style>
        /* Ajoutez vos styles CSS ici */
        .card {
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 20px;
            padding: 10px;
            background-color: #f9f9f9;
        }
        .card-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .card-subtitle {
            font-size: 14px;
            color: #666;
            margin-bottom: 10px;
        }
        .card-text {
            margin-bottom: 5px;
        }
        .btn-group {
            margin-top: 10px;
        }
        /* Ajoutez d'autres styles selon vos besoins */
    </style>
</head>
<body>
    <h1>Réclamations de type "{{ app.request.attributes.get('type') }}"</h1>
    {% for recl in Reclamation %}
        <div class="card">
            <div class="card-title">Nom : {{ recl.name }}</div>
            {{ recl.updated|date('d-m-Y') }}

            <div>Type : {{ recl.type }}</div>
            <div>Commentaire : {{ recl.commentaire }}</div>
        </div>
    {% else %}
        <p>Aucune réclamation trouvée pour ce type.</p>
    {% endfor %}
</body>
</html>
