<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <style>
        body { 
            font-family: DejaVu Sans, sans-serif; 
            margin: 0;
            padding: 0;
            color: #333;
        }
        .container {
            padding: 40px;
        }
        .header {
            background: #198754;
            color: white;
            padding: 30px;
            text-align: center;
            margin-bottom: 40px;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 20px;
        }
        .event-image {
            width: 100%;
            max-height: 300px;
            object-fit: cover;
            margin: 20px 0;
            border-radius: 10px;
        }
        .confirmation-box {
            border: 2px solid #198754;
            padding: 20px;
            margin: 20px 0;
            border-radius: 10px;
            background: #f8f9fa;
        }
        .details-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 20px 0;
        }
        .detail-item {
            padding: 10px;
            background: #fff;
            border-radius: 5px;
            border-left: 3px solid #198754;
        }
        .badge {
            background: #198754;
            color: white;
            padding: 5px 15px;
            border-radius: 15px;
            font-size: 14px;
            display: inline-block;
        }
        h1 { color: white; margin: 0; font-size: 28px; }
        h2 { color: #198754; margin-top: 30px; }
        h3 { color: #198754; margin-bottom: 10px; }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #eee;
            text-align: center;
            color: #666;
            font-size: 12px;
        }
        .qr-code {
            text-align: center;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ absolute_url(asset('img/logo.png')) }}" alt="MeetNTrip Logo" class="logo">
        <h1>Event Confirmation</h1>
        <span class="badge">{{ event.type }}</span>
    </div>

    <div class="container">
        <div class="confirmation-box">
            <h2>{{ event.nom }}</h2>
            {% if event.imagePath %}
                <img src="{{ absolute_url(asset('uploads/events/' ~ event.imagePath)) }}" 
                     alt="{{ event.nom }}" 
                     class="event-image">
            {% endif %}
        </div>

        <div class="details-grid">
            <div class="detail-item">
                <strong>📍 Location</strong><br>
                {{ event.lieuEvenement }}
            </div>
            <div class="detail-item">
                <strong>👥 Number of Guests</strong><br>
                {{ event.nombreInvite }}
            </div>
            <div class="detail-item">
                <strong>📅 Start Date</strong><br>
                {{ event.dateDebut|date('F d, Y') }}
            </div>
            <div class="detail-item">
                <strong>📅 End Date</strong><br>
                {{ event.dateFin|date('F d, Y') }}
            </div>
            <div class="detail-item">
                <strong>💰 Budget</strong><br>
                ${{ event.budgetPrevu|number_format(2) }}
            </div>
            <div class="detail-item">
                <strong>🆔 Event ID</strong><br>
                #{{ event.id }}
            </div>
        </div>

        {% if event.description %}
        <div class="confirmation-box">
            <h3>Event Description</h3>
            <p>{{ event.description }}</p>
        </div>
        {% endif %}

        {% if event.activities %}
        <div class="confirmation-box">
            <h3>Planned Activities</h3>
            <p>{{ event.activities }}</p>
        </div>
        {% endif %}

        <div class="qr-code">
            <img src="{{ absolute_url(path('client_event_qrcode', {'id': event.id})) }}" 
                 alt="Event QR Code" 
                 style="width: 150px;">
        </div>

        <div class="footer">
            <p>This document confirms your event registration with MeetNTrip.</p>
            <p>Generated on {{ "now"|date('F d, Y \a\t H:i') }}</p>
            <p>For any inquiries, please contact support@meetntrip.com</p>
        </div>
    </div>
</body>
</html>